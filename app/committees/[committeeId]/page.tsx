import React from "react";


export default async function Committee({params}: { params: { committeeId: string } }) {
    // const committee: Member = await getCommittee(params.committeeId, headers().get("Authorization")!)

    return (
        <>
        </>
    )
}
